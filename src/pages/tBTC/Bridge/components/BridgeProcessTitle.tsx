import { FC } from "react"
import { Icon, Stack, LabelSm } from "@threshold-network/components"

export const BridgeProcessTitle: FC = (props) => {
  return <Stack direction="row" mb={8} align={"center"} {...props}></Stack>
}
